import {RotateCcw,Plus} from 'lucide-react';import {useState} from 'react';import {azkar} from '../data/azkar';
export default function Azkar(){return <div><div className="mb-7"><div className="text-sm text-gold">وردك اليومي</div><h1 className="text-3xl font-extrabold">الأذكار</h1><p className="mt-2 text-sm text-muted">أذكار مرتبة مع عداد تكرار بسيط يحفظ تقدمك في الجلسة.</p></div><div className="grid gap-5 lg:grid-cols-2">{azkar.map(z=><section id={z.id} key={z.id} className="surface"><h2 className="font-arabic text-2xl font-bold">{z.title}</h2><div className="mt-5 space-y-4">{z.items.map(([text,count],i)=><Dhikr key={i} text={text as string} target={count as number}/>)}</div></section>)}</div></div>}
function Dhikr({text,target}:{text:string;target:number}){const [n,setN]=useState(0);return <div className="rounded-2xl border border-forest/10 p-4"><p className="font-arabic text-lg leading-9">{text}</p><div className="mt-4 flex items-center justify-between gap-3"><div className="flex-1"><div className="flex justify-between text-xs text-muted"><span>التقدم</span><span>{Math.min(n,target)} / {target}</span></div><div className="mt-2 h-2 rounded-full bg-forest/10"><div className="h-full rounded-full bg-gold transition-all" style={{width:`${Math.min(100,n/target*100)}%`}}/></div></div><button className="grid h-11 w-11 place-items-center rounded-xl bg-forest text-paper" onClick={()=>setN(x=>Math.min(target,x+1))} aria-label="زيادة"><Plus/></button><button className="icon-btn" onClick={()=>setN(0)} aria-label="إعادة"><RotateCcw size={17}/></button></div></div>}
