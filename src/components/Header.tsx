import {Moon,Sun,Search,Settings,BookOpen,Menu} from 'lucide-react';import {Link} from 'react-router-dom';import {useApp} from '../contexts/AppContext';
export default function Header({onMenu}:{onMenu:()=>void}){const {settings,setSettings}=useApp();const dark=settings.theme==='dark';return <header className="sticky top-0 z-40 border-b border-forest/10 bg-paper/90 backdrop-blur dark:border-white/10 dark:bg-[#081f1a]/90"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4"><div className="flex items-center gap-3"><button className="icon-btn md:hidden" onClick={onMenu} aria-label="فتح القائمة"><Menu/></button><Link to="/" className="flex items-center gap-2 font-bold text-forest dark:text-paper"><span className="grid h-10 w-10 place-items-center rounded-xl bg-forest text-gold"><BookOpen size={22}/></span><span className="text-lg">القرآن الكريم</span></Link></div><div className="flex items-center gap-1"><Link to="/search" className="icon-btn" aria-label="البحث"><Search/></Link><button className="icon-btn" onClick={()=>setSettings({...settings,theme:dark?'light':'dark'})} aria-label="الوضع الليلي">{dark?<Sun/>:<Moon/>}</button><Link to="/settings" className="icon-btn" aria-label="الإعدادات"><Settings/></Link></div></div></header>}
