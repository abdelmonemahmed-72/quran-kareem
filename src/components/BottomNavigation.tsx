import {Home,BookOpen,Search,Award,MoreHorizontal} from 'lucide-react';import {NavLink} from 'react-router-dom';
const items=[['/','الرئيسية',Home],['/quran','القرآن',BookOpen],['/search','البحث',Search],['/azkar','الأذكار',Award],['/settings','المزيد',MoreHorizontal]] as const;export default function BottomNavigation(){return <nav className="fixed bottom-0 left-0 right-0 z-40 grid grid-cols-5 border-t border-forest/10 bg-paper/95 backdrop-blur md:hidden dark:border-white/10 dark:bg-[#081f1a]/95">{items.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`bottom-link ${isActive?'active':''}`}><Icon size={19}/><span>{label}</span></NavLink>)}</nav>}
